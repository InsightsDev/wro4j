addons:
  artifacts:
    debug: true
    paths:
    - libs.zip
    - build.zip
    - testbuild.zip
    target_paths: 438e1348-f2c7-4e72-bc24-88c411247d5c
after_success:
- ./df-build.sh
before_install:
- sed -i.bak -e 's|https://nexus.codehaus.org/snapshots/|https://oss.sonatype.org/content/repositories/codehaus-snapshots/|g'
  $HOME/.m2/settings.xml
cache:
  directories:
  - $HOME/.m2
env: MAVEN_OPTS=" -Xmx2048m -Xms768m -XX:MaxPermSize=512m"
jdk:
- oraclejdk8
language: java
script: mvn clean test
# Modified by Insights Service at 2016-06-15 13:13:30.508461
